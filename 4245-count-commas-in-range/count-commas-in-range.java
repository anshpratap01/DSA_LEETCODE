class Solution {
    public int countCommas(int n) {
        String s = String.valueOf(n);
        if(s.length() <= 3){
            return 0;
        }
        // char ch = s.charAt(s.length()-1);
        // int nn = ch-'0';
        // return nn+1;
        //for(int i = 100)
        
        return n - 999;
        
    }
}